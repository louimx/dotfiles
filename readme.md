# Nvim

Recommended way is to download the latest version or neovim via official site.
You will need ripgrep for fuzzy searching*

## Install
Simply run the install.sh shell script and it will create symlinks to necessary configs and files. The script is idempotent so its safe to run multiple times.
